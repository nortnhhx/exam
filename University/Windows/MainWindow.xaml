<Window x:Class="University.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University.Windows"
        mc:Ignorable="d"
        Title="Университет" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
         Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="LightGray">
            <Button Content="Преподаватели" Width="120" Margin="5" Click="TeachersButton_Click"/>
            <Button Content="Нагрузка" Width="120" Margin="5" Click="WorkloadButton_Click"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                <TextBlock x:Name="UserInfoTextBlock"/>
                <TextBlock x:Name="GuestLabel" Text=" (Гость)" Foreground="Gray" 
                       Visibility="Collapsed"/>
            </StackPanel>

            <Button Content="Выйти" Width="80" Height="30" Margin="10,5" HorizontalAlignment="Right"
                Click="LogoutButton_Click"/>
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Row="1"/>
    </Grid>
</Window>
