<Window x:Class="University.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University.Windows"
        mc:Ignorable="d"
        Title="Вход" Height="450" Width="450"
        WindowStartupLocation="CenterScreen" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <TextBlock Text="Вход в систему" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Text="Логин:"/>
        <TextBox Grid.Row="2" x:Name="UsernameTextBox" Height="30" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="3" Text="Пароль:"/>

        <Grid Grid.Row="4" Height="30" Margin="0,5,0,10">
            <PasswordBox x:Name="PasswordBox" 
                        Visibility="Visible"/>
            <TextBox x:Name="PasswordTextBox" 
                    Visibility="Collapsed"/>
        </Grid>

        <CheckBox Grid.Row="5" x:Name="ShowPasswordCheckBox" 
                    Content="Показать пароль"
                    Margin="0,0,0,10"
                    Checked="ShowPasswordCheckBox_Checked" 
                    Unchecked="ShowPasswordCheckBox_Unchecked"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Войти" Width="100" Height="30" Margin="5" Click="LoginButton_Click"/>
            <Button Content="Регистрация" Width="100" Height="30" Margin="5" Click="RegisterButton_Click"/>
        </StackPanel>

        <Button Grid.Row="7" Content="Войти как гость" Width="150"
                HorizontalAlignment="Center" Click="GuestButton_Click" 
                Background="LightGray" FontWeight="Bold" Height="50"/>

        <TextBlock Grid.Row="9" x:Name="MessageTextBlock" 
                    Foreground="Red" Margin="0,10" 
                    HorizontalAlignment="Center"/>
        </Grid>
</Window>
